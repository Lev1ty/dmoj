#define _CRT_SECURE_NO_WARNINGS

#include <iostream>
#include <string>
#include <algorithm>

using namespace std;

string s, l, r;

int main() {
	for (int i = 0; i < 10; ++i) {
		getline(cin, s);
		l = r = s;
	}
}